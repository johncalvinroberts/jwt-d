{
  "scripts": {
    "start": "wmr",
    "build": "wmr build --prerender",
    "serve": "wmr serve",
    "flint": "eslint . --fix --ignore-path .gitignore && prettier --write . --ignore-path .gitignore"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "alias": {
    "react": "preact/compat",
    "react-dom": "preact/compat"
  },
  "dependencies": {
    "eslint": "^7.20.0",
    "eslint-config-preact": "^1.1.3",
    "prettier": "^2.2.1",
    "preact-iso": "^0.3.0"
  },
  "devDependencies": {
    "wmr": "^1.0.0"
  }
}
