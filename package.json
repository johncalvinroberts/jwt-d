{
  "scripts": {
    "start": "wmr",
    "build": "wmr build --prerender",
    "serve": "wmr serve",
    "flint": "eslint . --ignore-path .gitignore && prettier --write . --ignore-path .gitignore"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "alias": {
    "react": "preact/compat",
    "react-dom": "preact/compat"
  },
  "dependencies": {
    "eslint": "^7.20.0",
    "eslint-config-preact": "^1.1.3",
    "preact-iso": "^0.2.0"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "wmr": "^1.0.0"
  }
}
